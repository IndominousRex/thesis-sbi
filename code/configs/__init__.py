"""Experiment configuration for SBI runs.

The central ExperimentConfig dataclass captures all hyperparameters for
data generation, training, evaluation, and output. Import via::

    from configs.config import ExperimentConfig, PARAMETER_ORDER
"""
